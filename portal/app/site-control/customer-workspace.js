"use client";
import {Nav} from "../shell";import {withSite} from "../site-context";import useCustomerControl from "./use-customer-control";import ui from "../portal.module.css";
function plain(v){return String(v||"Feature").replaceAll("_"," ").replace(/\b\w/g,c=>c.toUpperCase())}function status(c){if(c.verdict==="unsupported")return["s-bad","Not available"];if(c.verdict==="supported"&&c.evidence_class==="FIELD_VERIFIED")return["s-ok","Available"];if(c.verdict==="supported"||c.verdict==="by_camera")return["s-warn","Needs confirmation"];return["s-unk","Not yet confirmed"]}
export default function CustomerControl(){const c=useCustomerControl(),d=c.data||{},system=d.recorder||{},conn=d.connectivity||{},caps=d.capabilities||[],enabled=(d.site_control_enabled??d.flags?.site_control_enabled??d.enabled)!==false;return <div className="shell"><Nav active="Site Control" email={c.email} currentSiteId={c.siteId}/><main className="main"><header className="target-page-head"><div><div className="target-eyebrow">Camera system changes</div><h1>Manage supported settings at {c.site?.name||"this site"}.</h1><p>WatchLog checks what this camera system can support before suggesting any change. Nothing is changed without approval.</p></div></header>{c.error&&<div className="err">{c.error}</div>}{!c.data?<div className={ui.emptyCard}>Checking this camera system…</div>:!enabled?<section className={ui.featureCard}><span className="pill s-unk">Not enabled</span><h2 style={{fontSize:25,margin:"12px 0 8px",letterSpacing:"-.03em"}}>Camera-system changes are not enabled for this site.</h2><p style={{fontSize:14,margin:0,maxWidth:720}}>You can still use WatchLog monitoring, reports and recommendations. Direct camera-system changes remain unavailable.</p></section>:<><section className={ui.featureCard}><span className={`pill ${conn.agent_online?"s-ok":"s-warn"}`}>{conn.agent_online?"Connected":"Needs attention"}</span><h2 style={{fontSize:25,margin:"12px 0 8px",letterSpacing:"-.03em"}}>{[system.vendor,system.model].filter(Boolean).join(" ")||"Camera system"}</h2><p style={{fontSize:14,margin:0,maxWidth:720}}>WatchLog has checked the current system information and will only suggest changes it can safely confirm.</p></section><div className={ui.sectionHead}><div><h2>Available settings</h2><p>What WatchLog currently knows about this camera system.</p></div></div><section className={ui.card}>{caps.length?<div className={ui.splitList}>{caps.map(x=>{const[s,l]=status(x);return <div className={ui.listRow} key={x.capability}><span className={`pill ${s}`}>{l}</span><div><strong>{plain(x.capability)}</strong><small>{l==="Available"?"WatchLog has confirmed this setting on this system.":l==="Not available"?"This setting is not available on this system.":"WatchLog will confirm this before proposing a change."}</small></div></div>})}</div>:<div className={ui.emptyCard}>WatchLog has not confirmed any configurable camera-system settings yet.</div>}</section></>}<div className={ui.sectionHead}><div><h2>Want to change something?</h2><p>Tell WatchLog what you want to achieve. It will explain what is possible before asking for approval.</p></div><a className={ui.primaryLink} href={withSite(`/ai/?prompt=${encodeURIComponent("I want to change a camera-system setting. Explain what is safely available at this site and ask for approval before any change.")}`,c.siteId)}>Ask WatchLog</a></div></main></div>}
