import {setupPill} from "../shell";import {withSite} from "../site-context";import ui from "../portal.module.css";
function fmt(ts){return ts?new Date(ts).toLocaleString():"No activity yet"}
export default function SiteList({settings}){return <>{<section className="settings-site-grid">{settings.sites.map(s=>{const[label,cls]=setupPill(s.setup_state,s.online);return <article className={`settings-site-card ${s.id===settings.siteId?"active":""}`} key={s.id} onClick={()=>settings.choose(s.id)}><div className="settings-site-head"><div><b>{s.name}</b><small>{fmt(s.last_event)}</small></div><span className={`pill ${cls}`}>{label}</span></div><div className="settings-site-facts"><div><span>Cameras</span><b>{s.cameras??0}</b></div><div><span>Timezone</span><b>{s.timezone||"Not set"}</b></div></div></article>})}{!settings.sites.length&&<div className={ui.emptyCard}>No sites yet. Add your first site above.</div>}</section>}{settings.current&&<><div className={ui.sectionHead}><div><h2>{settings.current.name}</h2><p>What would you like to do?</p></div></div><section className={ui.threeCol}><a className={ui.roleCard} href={withSite("/setup/",settings.current.id)}><strong>Guided Setup</strong><p>Connect WatchLog and choose what to monitor.</p><span>Continue setup →</span></a><a className={ui.roleCard} href={withSite("/site-health/",settings.current.id)}><strong>Site Health</strong><p>Check camera and recording status.</p><span>Open health →</span></a><a className={ui.roleCard} href={withSite("/ai/",settings.current.id)}><strong>WatchLog AI</strong><p>Ask what happened or adjust monitoring.</p><span>Ask WatchLog →</span></a></section></>}</>}
