"use client";
import {Nav} from "../shell";import AddSite from "./add-site";import SiteList from "./site-list";import useCustomerSettings from "./use-customer-settings";import ui from "../portal.module.css";
export default function CustomerSettingsV2(){const s=useCustomerSettings();return <div className="shell"><Nav active="Settings" email={s.email} currentSiteId={s.siteId}/><main className="main"><header className="target-page-head"><div><div className="target-eyebrow">Settings</div><h1>Account and sites</h1><p>Manage your account, add sites and open the right WatchLog view for each location.</p></div><div className="target-actions"><a className={ui.secondaryLink} href="/settings/account/">Billing & agreements</a></div></header>{s.error&&<div className="err">{s.error}</div>}<div className={ui.sectionHead}><div><h2>Your sites</h2><p>Add a site here, then Guided Setup will help connect its cameras.</p></div></div><AddSite/><SiteList settings={s}/></main></div>}
