"use client";
import {useEffect,useState} from "react";
import {supabase,say} from "../../lib/supabase";
import {Nav,requireTenant,setupPill} from "../shell";
import {rememberSite,selectedSiteId,withSite} from "../site-context";
import ui from "../portal.module.css";
function fmt(ts){return ts?new Date(ts).toLocaleString():"No activity yet";}
export default function CustomerSettings(){
 const[email,setEmail]=useState(""),[sites,setSites]=useState([]),[siteId,setSiteId]=useState(""),[error,setError]=useState("");
 useEffect(()=>{(async()=>{const g=await requireTenant();if(!g)return;setEmail(g.session.user.email||"");const{s,data,error}=await (async()=>{const r=await supabase().rpc("wl_sites");return{s:r,data:r.data,error:r.error}})();if(error){setError(say(error));return;}const list=data||[];setSites(list);const id=new URLSearchParams(location.search).get("site")||selectedSiteId()||list[0]?.id||"";if(id){setSiteId(id);rememberSite(id,list.find(x=>x.id===id)?.name||"");}})();},[]);
 const current=sites.find(s=>s.id===siteId);
 return <div className="shell"><Nav active="Settings" email={email} currentSiteId={siteId}/><main className="main"><header className="target-page-head"><div><div className="target-eyebrow">Settings</div><h1>Account and sites</h1><p>Manage your account and open the right WatchLog view for each site.</p></div><div className="target-actions"><a className={ui.secondaryLink} href="/settings/account/">Billing & agreements</a></div></header>{error&&<div className="err">{error}</div>}<div className={ui.sectionHead}><div><h2>Your sites</h2><p>Site setup and camera changes stay in Guided Setup.</p></div><a className={ui.primaryLink} href="/setup/">Add or set up a site</a></div><section className="settings-site-grid">{sites.map(s=>{const[label,cls]=setupPill(s.setup_state,s.online);return <article className={`settings-site-card ${s.id===siteId?"active":""}`} key={s.id} onClick={()=>{setSiteId(s.id);rememberSite(s.id,s.name);history.replaceState(null,"",withSite("/settings/",s.id));}}><div className="settings-site-head"><div><b>{s.name}</b><small>{fmt(s.last_event)}</small></div><span className={`pill ${cls}`}>{label}</span></div><div className="settings-site-facts"><div><span>Cameras</span><b>{s.cameras??0}</b></div><div><span>Timezone</span><b>{s.timezone||"Not set"}</b></div></div></article>})}</section>{current&&<><div className={ui.sectionHead}><div><h2>{current.name}</h2><p>What would you like to do?</p></div></div><section className={ui.threeCol}><a className={ui.roleCard} href={withSite("/setup/",current.id)}><strong>Guided Setup</strong><p>Connect WatchLog and choose what to monitor.</p><span>Continue setup →</span></a><a className={ui.roleCard} href={withSite("/site-health/",current.id)}><strong>Site Health</strong><p>Check camera and recording status.</p><span>Open health →</span></a><a className={ui.roleCard} href={withSite("/ai/",current.id)}><strong>WatchLog AI</strong><p>Ask what happened or adjust monitoring.</p><span>Ask WatchLog →</span></a></section></>}</main></div>;
}