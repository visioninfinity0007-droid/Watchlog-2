"use client";
import {useEffect,useState} from "react";
import {supabase,say} from "../../lib/supabase";
import s from "./customer.module.css";
const TYPES=[["office","Office"],["warehouse","Warehouse"],["factory","Factory"],["retail","Retail"],["restaurant","Restaurant"],["clinic","Clinic"],["other","Other"]];
const DAYS=[[1,"Mon"],[2,"Tue"],[3,"Wed"],[4,"Thu"],[5,"Fri"],[6,"Sat"],[7,"Sun"]];
export default function SiteDetails({siteId,ctx,canManage,onSaved,onError}){const[siteType,setSiteType]=useState("");const[open,setOpen]=useState("08:00");const[close,setClose]=useState("19:00");const[days,setDays]=useState([1,2,3,4,5]);const[busy,setBusy]=useState(false);useEffect(()=>{const b=ctx?.business_context||{};if(b.site_type)setSiteType(b.site_type);if(b.open_time)setOpen(String(b.open_time).slice(0,5));if(b.close_time)setClose(String(b.close_time).slice(0,5));if(Array.isArray(b.working_days)&&b.working_days.length)setDays(b.working_days.map(Number))},[ctx]);async function save(){if(!canManage||!siteType)return;setBusy(true);const{error}=await supabase().rpc("wl_upsert_site_context",{p_site_id:siteId,p_site_type:siteType,p_open_time:open,p_close_time:close,p_overnight:false,p_working_days:days,p_entrance_camera_ids:[],p_reception_camera_ids:[],p_management_camera_ids:[],p_critical_camera_ids:[],p_restricted_purposes:null,p_reporting_prefs:null,p_notification_prefs:null});setBusy(false);if(error){onError(say(error));return}onSaved("Site details saved.")}
return <section className={s.card}><h3>What kind of place is this?</h3><p>This helps WatchLog understand normal hours and what should happen outside them.</p><div className={s.choices}>{TYPES.map(([v,l])=><button key={v} className={`${s.choice} ${siteType===v?s.choiceActive:""}`} onClick={()=>setSiteType(v)} disabled={!canManage}>{l}</button>)}</div><div className={s.row}><label>Opens<input type="time" value={open} onChange={e=>setOpen(e.target.value)} disabled={!canManage}/></label><label>Closes<input type="time" value={close} onChange={e=>setClose(e.target.value)} disabled={!canManage}/></label></div><label>Working days</label><div className={s.days}>{DAYS.map(([n,l])=><button key={n} className={days.includes(n)?s.active:""} onClick={()=>setDays(x=>x.includes(n)?x.filter(d=>d!==n):[...x,n].sort())} disabled={!canManage}>{l}</button>)}</div>{canManage&&<button onClick={save} disabled={busy||!siteType}>{busy?"Saving…":"Continue"}</button>}</section>}
