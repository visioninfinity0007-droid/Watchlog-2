"use client";
import {Nav} from "../shell";import useSavedVideo from "./use-saved-video";import SavedVideoForm from "./saved-video-form";import SavedVideoHistory from "./saved-video-history";import SavedVideoDetail from "./saved-video-detail";import styles from "./archive.module.css";
export default function CustomerArchive(){const v=useSavedVideo();return <div className="shell"><Nav active="Archive" email={v.email} currentSiteId={v.siteId}/><main className="main"><header className="target-page-head"><div><div className="target-eyebrow">Saved Video</div><h1>Review earlier video from {v.site?.name||"this site"}.</h1><p>Search a time period and recover useful activity without mixing it with live monitoring.</p></div></header>{v.error&&<div className="err">{v.error}</div>}<section className={styles.grid}><SavedVideoForm v={v}/><SavedVideoHistory items={v.items} onOpen={v.open}/></section><SavedVideoDetail detail={v.detail} onClose={()=>v.setDetail(null)}/></main></div>}
