import styles from "./archive.module.css";
function when(v){return v?new Date(v).toLocaleString():"—"}function label(v){return String(v||"Requested").replaceAll("_"," ").replace(/^./,c=>c.toUpperCase())}
export default function SavedVideoHistory({items,onOpen}){return <div className="panel"><div className={styles.pTitle}>Recent searches</div><div className={styles.tableWrap}>{items.length?<table><thead><tr><th>Requested</th><th>Period</th><th>Cameras</th><th>Status</th><th></th></tr></thead><tbody>{items.map(x=><tr key={x.id}><td>{when(x.requested_at)}</td><td className="muted">{when(x.from_ts)} → {when(x.to_ts)}</td><td>{(x.camera_ids||[]).length}</td><td><span className={`pill ${x.status==="complete"?"s-ok":x.status==="failed"?"s-bad":"s-warn"}`}>{label(x.status)}</span></td><td><button className="ghost small" onClick={()=>onOpen(x.id)}>View</button></td></tr>)}</tbody></table>:<div className="empty">No saved-video searches for this site yet.</div>}</div></div>}
