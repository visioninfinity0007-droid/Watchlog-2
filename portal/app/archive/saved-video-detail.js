import styles from "./archive.module.css";
function when(v){return v?new Date(v).toLocaleString():"—"}function human(v){return String(v||"Activity").replaceAll("_"," ").replace(/^./,c=>c.toUpperCase())}
export default function SavedVideoDetail({detail,onClose}){if(!detail)return null;return <section className={styles.detail}><div className={styles.sectionHead}><h2>Recovered activity</h2><button className="ghost small" onClick={onClose}>Close</button></div><div className="panel"><div className={styles.meta}><div><span className="muted">Video period</span><b>{when(detail.scan?.from_ts)} → {when(detail.scan?.to_ts)}</b></div><div><span className="muted">Status</span><b>{human(detail.scan?.status)}</b></div><div><span className="muted">Source</span><b>Recovered from saved video</b></div></div><div className={styles.tableWrap}>{(detail.results||[]).length?<table><thead><tr><th>Found</th><th>Activity</th><th>Confidence</th><th>Source</th></tr></thead><tbody>{detail.results.map(r=><tr key={r.id}><td>{when(r.recovered_at)}</td><td>{human(r.result_type)}</td><td>{r.confidence??"—"}</td><td>Saved video</td></tr>)}</tbody></table>:<div className="empty">No matching activity has been recovered from this video period yet.</div>}</div></div></section>}
