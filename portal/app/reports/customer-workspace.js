"use client";
import {Nav} from "../shell";import {withSite} from "../site-context";import Mark from "../mark";import ui from "../portal.module.css";import useReport from "./use-report";
const VIEWS=[["daily","Today"],["monthly","30 days"],["executive","Executive"]];
export default function CustomerReports(){const r=useReport();return <div className="shell"><Nav active="Reports" email={r.email} currentSiteId={r.siteId}/><main className="main"><header className="target-page-head"><div><div className="target-eyebrow">Reports</div><h1>Management report</h1><p>What happened, what needs attention, and whether WatchLog was watching reliably.</p></div><div className="target-actions"><a className={ui.secondaryLink} href={withSite("/reports/delivery/",r.siteId)}>Delivery</a></div></header>{r.error&&<div className="err">{r.error}</div>}<div className={ui.tabs}>{VIEWS.map(([k,l])=><button key={k} className={`${ui.tab} ${r.view===k?ui.tabActive:""}`} onClick={()=>r.setView(k)}>{l}</button>)}</div><section className="daily-report-card"><div className="daily-report-head"><Mark size={26}/><b>{VIEWS.find(([k])=>k===r.view)?.[1]} report</b></div><div className="daily-report-body">{r.busy?<div className="empty">Preparing the report…</div>:<p style={{whiteSpace:"pre-wrap",margin:0}}>{r.answer||"No report is available yet."}</p>}</div></section><div className={ui.sectionHead}><div><h2>Need more detail?</h2><p>Ask WatchLog about any part of this report.</p></div><a className={ui.primaryLink} href={withSite("/ai/",r.siteId)}>Ask WatchLog</a></div></main></div>}
