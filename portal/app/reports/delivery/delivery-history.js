import ui from "../../portal.module.css";
export default function DeliveryHistory({items}){return <><div className={ui.sectionHead}><div><h2>Recent deliveries</h2><p>Latest report delivery results.</p></div></div><div className="panel"><div className={ui.tableWrap}>{items.length?<table><thead><tr><th>Date</th><th>Site</th><th>Channel</th><th>Status</th></tr></thead><tbody>{items.slice(0,50).map((h,i)=><tr key={i}><td>{h.date}</td><td>{h.site}</td><td>{h.channel}</td><td><span className={`pill ${h.status==="sent"?"s-ok":h.status==="failed"?"s-bad":"s-unk"}`}>{h.status}</span></td></tr>)}</tbody></table>:<div className="empty">No deliveries yet.</div>}</div></div></>}
