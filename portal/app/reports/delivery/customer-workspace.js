"use client";
import {Nav} from "../../shell";import {withSite} from "../../site-context";import ui from "../../portal.module.css";import useDelivery from "./use-delivery";import RecipientForm from "./recipient-form";import RecipientList from "./recipient-list";import DeliveryHistory from "./delivery-history";
export default function CustomerDelivery(){const d=useDelivery();return <div className="shell"><Nav active="Reports" email={d.email} currentSiteId={d.siteId}/><main className="main"><header className="target-page-head"><div><div className="target-eyebrow">Report delivery</div><h1>Who receives WatchLog reports?</h1><p>Manage recipients and check whether scheduled reports arrived.</p></div><div className="target-actions"><a className={ui.secondaryLink} href={withSite("/reports/",d.siteId)}>Back to report</a></div></header>{d.error&&<div className="err">{d.error}</div>}<RecipientForm delivery={d}/><RecipientList delivery={d}/><DeliveryHistory items={d.history}/></main></div>}
