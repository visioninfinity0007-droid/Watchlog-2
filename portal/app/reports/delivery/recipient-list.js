import ui from "../../portal.module.css";
export default function RecipientList({delivery}){const canManage=delivery.role==="owner"||delivery.role==="admin";return <><div className={ui.sectionHead}><div><h2>Recipients</h2><p>People currently set to receive reports.</p></div></div><div className="panel"><div className={ui.tableWrap}>{delivery.recipients.length?<table><thead><tr><th>Name</th><th>Channel</th><th>Site</th><th>Status</th><th></th></tr></thead><tbody>{delivery.recipients.map(r=><tr key={r.id}><td>{r.name||"Recipient"}</td><td>{r.channel}</td><td>{r.site||"All sites"}</td><td><span className={`pill ${r.enabled?"s-ok":"s-unk"}`}>{r.enabled?"Active":"Paused"}</span></td><td>{canManage&&<div className={ui.inlineActions}><button className="ghost small" onClick={()=>delivery.toggle(r.id,r.enabled)}>{r.enabled?"Pause":"Resume"}</button><button className="btn-danger" onClick={()=>delivery.remove(r.id)}>Remove</button></div>}</td></tr>)}</tbody></table>:<div className="empty">No report recipients yet.</div>}</div></div></>}
