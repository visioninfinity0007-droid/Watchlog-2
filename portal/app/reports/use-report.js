"use client";
import {useEffect,useState} from "react";import {supabase,say} from "../../lib/supabase";import {requireTenant} from "../shell";import {selectedSiteId} from "../site-context";
const PROMPTS={daily:"Give me today's management report. Cover monitoring reliability, important activity, security attention and the priority action.",monthly:"Summarize the last 30 days for management: monitoring reliability, important patterns, incidents and the most important change.",executive:"Give me a concise executive summary: monitoring reliability, security attention, meaningful patterns and the highest-priority action."};
export default function useReport(){const[email,setEmail]=useState(""),[siteId,setSiteId]=useState(""),[view,setView]=useState("daily"),[answer,setAnswer]=useState(""),[busy,setBusy]=useState(false),[error,setError]=useState("");useEffect(()=>{(async()=>{const g=await requireTenant();if(!g)return;setEmail(g.session.user.email||"");setSiteId(new URLSearchParams(location.search).get("site")||selectedSiteId())})()},[]);useEffect(()=>{if(!siteId)return;(async()=>{setBusy(true);setAnswer("");const r=await supabase().functions.invoke("watchlog-ai",{body:{prompt:PROMPTS[view],site_id:siteId,conversation_id:null}});setBusy(false);if(r.error||r.data?.error){setError(r.data?.message||say(r.error)||"WatchLog could not load this report.");return}setAnswer(r.data.answer||"")})()},[siteId,view]);return{email,siteId,view,setView,answer,busy,error}}
