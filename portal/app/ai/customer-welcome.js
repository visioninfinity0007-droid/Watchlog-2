import Mark from "../mark";import {prompts} from "./customer-prompts";
export default function CustomerWelcome({site,ctx,send,styles}){return <div className={styles.welcome}><div className={styles.aiMark}><Mark size={42}/></div><h1>How can I help with {site?.name||"your site"}?</h1><div className={styles.starters}>{prompts(ctx,site?.name).map(x=><button key={x} onClick={()=>send(x)}>{x}<span>→</span></button>)}</div></div>}
