# --- credentials: never commit ------------------------------------------
.env
*.env
!.env.example
*.pem
*.key

# --- internal working notes, not client deliverables --------------------
# Commercial context, security incident detail and open questions live
# here. This repo is in the client's GitHub org; these do not belong in it.
_memory/
AGENTS.md
router.md

# --- dependencies and build output --------------------------------------
node_modules/
.next/
out/
build/
dist/
.turbo/
__pycache__/
*.pyc

# --- scratch -------------------------------------------------------------
.tmp/
*.tmp
*.bak

# Raw supplied images. Only the processed output in themes/watchlog/img/
# is version-controlled.
_drop/
dist-installer/
prototype/models/
prototype/build_out.log
